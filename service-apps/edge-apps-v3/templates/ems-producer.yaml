{{- range .Values.edges }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ems-producer-{{ .name }}
  namespace: edge-apps  
spec:
  replicas: 1          
  selector:
    matchLabels:
      app: ems-producer
      edge: {{ .name }}
  template:
    metadata:
      labels:
        app: ems-producer
        edge: {{ .name }}
    spec:
      serviceAccountName: ems-producer-sa
      nodeSelector:
        kubernetes.io/hostname: {{ if index $.Values.maintenance_mode .name }}{{ $.Values.cloud_node_name | quote }}{{ else }}{{ .name | quote }}{{ end }}
      containers:
        - name: producer
          image: plubpo/ems-app:v8.7
          command: ["python", "producer.py"]
          imagePullPolicy: Always
          env:
            - name: DB_HOST
              value: "timescaledb-{{ .name }}-entry"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: timescaledb-{{ .name }}-app
                  key: username
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: timescaledb-{{ .name }}-app
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: timescaledb-{{ .name }}-app
                  key: dbname
            - name: MANUAL_DATA_COUNT
              value: "0"
            - name: DIVIDER_VALUE
              value: "5"
{{- end }}